package com.uniftec.usermap;

import androidx.appcompat.app.AppCompatActivity;
/*
import com.google.android.gms.maps.GoogleMap;
import com.google.android.gms.maps.OnMapReadyCallback;
import com.google.android.gms.maps.SupportMapFragment;
import com.google.android.gms.maps.model.LatLng;
import com.google.android.gms.maps.model.MarkerOptions;
 */
import android.os.Bundle;

public class Maps extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_maps);
    }

    /*
    public void onMapReady(GoogleMap googleMap, String latlong) {
        mMap = googleMap;
        latLong = {pessoa.getLatitude() + pessoa.getLongitude()}

        // Add a marker in Sydney and move the camera
        LatLng marker = new LatLng(latLong);
        mMap.addMarker(new MarkerOptions().position(marker).title("A pessoa está nesse ponto"));
        mMap.moveCamera(CameraUpdateFactory.newLatLng(marker));
*/


}